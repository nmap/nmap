[package]
name = "nmap-targets"
version = "0.1.0"
edition = "2021"

[dependencies]
nmap-net = { path = "../nmap-net" }
tokio = { version = "1.0", features = ["full"] }
anyhow = "1.0"
tracing = "0.1"
ipnet = "2.9"
dns-lookup = "2.0"