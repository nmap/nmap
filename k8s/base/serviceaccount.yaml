apiVersion: v1
kind: ServiceAccount
metadata:
  name: rmap
  labels:
    app: rmap
    component: api
automountServiceAccountToken: true
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: rmap-scanner
  labels:
    app: rmap
    component: scanner
  annotations:
    description: "Service account for R-Map scanner jobs with elevated permissions"
automountServiceAccountToken: true
